#----------------------------------------------------------------
# Microsoft Visual C++
#---------------------------------------------------------------

CC = cl -c -nologo -O -Ob2 -EHsc -TP
PLAIN_C = cl -c -nologo -O -Ob2 -EHsc -TC
LD = cl -nologo
LINKVER =

AR  = lib -nologo 
AR0 = lib -nologo /out:

