#----------------------------------------------------------------
# Microsoft Visual C++
#---------------------------------------------------------------

CC = cl -c -nologo -ML -O -Ob2 -EHsc -TP
PLAIN_C = cl -c -nologo -ML -O -Ob2 -EHsc -TC
LD = cl -nologo -ML
LINKVER = _ML

AR  = lib -nologo 
AR0 = lib -nologo /out:

